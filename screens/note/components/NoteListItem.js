import React, { memo } from "react";
import { Text, StyleSheet } from "react-native";

function NoteListItem() {
  return <Text>NoteListItem</Text>;
}

const styles = StyleSheet.create({});

export default memo(NoteListItem);
